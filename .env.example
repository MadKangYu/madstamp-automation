# =============================================================================
# Madstamp Automation - 환경변수 설정
# =============================================================================
# 이 파일을 .env로 복사한 후 실제 값으로 수정하세요.
# cp .env.example .env

# -----------------------------------------------------------------------------
# Supabase 데이터베이스
# -----------------------------------------------------------------------------
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# -----------------------------------------------------------------------------
# OpenRouter API (AI 이미지 분석)
# -----------------------------------------------------------------------------
# 모델: x-ai/grok-4.1-fast (OpenRouter Grok 4.1 Fast)
OPENROUTER_API_KEY=sk-or-v1-your-api-key
OPENROUTER_MODEL=x-ai/grok-4.1-fast

# 대체 모델 (Grok-4.1-fast 문제 시)
OPENROUTER_FALLBACK_MODEL=cerebras/llama-3.3-70b

# -----------------------------------------------------------------------------
# OCR.space API (텍스트 인식)
# -----------------------------------------------------------------------------
OCR_SPACE_API_KEY=your-ocr-space-api-key

# -----------------------------------------------------------------------------
# Gmail 설정
# -----------------------------------------------------------------------------
# 모니터링할 이메일 주소
TARGET_EMAIL=goopick@goopick.net

# 관리자 알림 이메일
ADMIN_EMAIL=richardowen7212@gmail.com

# -----------------------------------------------------------------------------
# Lovart AI 설정
# -----------------------------------------------------------------------------
# Lovart AI 세션 쿠키 (브라우저에서 로그인 후 추출)
LOVART_SESSION_COOKIE=your-session-cookie

# -----------------------------------------------------------------------------
# 서버 설정
# -----------------------------------------------------------------------------
# 서버 포트
PORT=8000

# 환경 (development, staging, production)
ENVIRONMENT=development

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# 회사 정보
# -----------------------------------------------------------------------------
COMPANY_NAME=Madstamp
BUSINESS_NUMBER=880-86-02373
COMPANY_PHONE=+82 10 5911 2822
